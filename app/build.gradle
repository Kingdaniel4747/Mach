apply plugin: "com.android.application"

def machVersionCode = (System.getenv("MACH_VERSION_CODE") ?: "16").toInteger()
def machVersionName = System.getenv("MACH_VERSION_NAME") ?: "1.15.0"

android {
    namespace "de.danberg.wachwerk"
    compileSdk 35

    defaultConfig {
        applicationId "de.danberg.wachwerk"
        minSdk 26
        targetSdk 35
        versionCode machVersionCode
        versionName machVersionName
    }

    signingConfigs {
        release {
            def keystorePath = System.getenv("MACH_KEYSTORE") ?: System.getenv("WACHWERK_KEYSTORE")
            if (keystorePath != null) {
                storeFile file(keystorePath)
                storePassword System.getenv("MACH_STORE_PASSWORD") ?: System.getenv("WACHWERK_STORE_PASSWORD")
                keyAlias System.getenv("MACH_KEY_ALIAS") ?: System.getenv("WACHWERK_KEY_ALIAS") ?: "wachwerk"
                keyPassword System.getenv("MACH_KEY_PASSWORD") ?: System.getenv("WACHWERK_KEY_PASSWORD")
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            signingConfig signingConfigs.release
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation "com.google.zxing:core:3.4.1"
    testImplementation "junit:junit:4.13.2"
    testImplementation "org.mockito:mockito-core:4.11.0"
    testImplementation "org.json:json:20240303"
}
